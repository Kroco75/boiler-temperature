# Troubleshooting

## Device does not connect to Wi-Fi

- Check `esphome/secrets.yaml`.
- Confirm the device is within Wi-Fi range.
- Use the fallback access point if needed.

## Temperature sensor is missing

- Check the 1-Wire data pin.
- Confirm the 4.7k pull-up resistor is installed.
- Verify sensor power and ground.

