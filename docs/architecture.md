# Architecture

This project contains ESPHome configuration for monitoring boiler temperature.

## Layout

- `esphome/boiler-temperature.yaml` is the main ESPHome entry point.
- `esphome/packages/` contains feature modules.
- `esphome/common/` contains reusable shared YAML fragments.
- `hardware/` contains wiring, schematics, and pinout notes.

